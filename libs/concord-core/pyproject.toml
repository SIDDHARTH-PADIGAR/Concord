[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "concord-core"
version = "0.1.0"
description = "Shared domain models, configuration, and cross-cutting concerns for the Concord reconciliation platform."
requires-python = ">=3.11"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/concord_core"]