"""Shared pytest fixtures for the Concord test suite.

Empty for now. This file exists so that pytest resolves a single,
consistent rootdir across `tests/unit` and (later) `tests/integration`,
and so that shared fixtures have an obvious home once domain code
exists to fixture.
"""
